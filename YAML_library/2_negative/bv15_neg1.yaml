# expect: Approval ballots may only use scores {0, 1}
# expect: invalid: Blake=5
# NEGATIVE — Approval ballot with a non-0/1 value.
# Approval allows only 0 or 1; one cell is 5.
# Original intent (BV15_neg1): Plurality with a 5 in the ballots (only 0/1 allowed).
# (Migrated Plurality -> Approval: the current engine's 0/1 method is Approval.)
# Current engine error: "Approval ballots may only use scores {0, 1}" + "(invalid: Blake=5)".
election_title: Basic — Approval with an out-of-range value
voting_method: Approval
num_winners: 1
ballots: |-
  Andre,Blake
  0,1
  0,5
  ~,~

# file: bv15_neg1.yaml
